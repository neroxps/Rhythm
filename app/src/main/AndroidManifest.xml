<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Images permission is OPTIONAL - only needed for enhanced album art display -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Optional permission for auto-update installation from GitHub -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" android:required="false" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" 
                     android:usesPermissionFlags="neverForLocation"
                     tools:targetApi="s" />

    <queries>
        <intent>
            <action android:name="android.media.browse.MediaBrowserService" />
        </intent>
        <intent>
            <action android:name="androidx.media3.session.MediaSessionService" />
        </intent>
    </queries>

    <application
        android:name=".RhythmApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/Theme.Rhythm"
        android:largeHeap="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="36">
        
        <!--
            Required for Macrobenchmark / Perfetto to measure frameDurationCpuMs.
            The benchmark runner operates as the ADB shell user; profileable=true
            allows it to attach Perfetto to this process.
        -->
        <profileable
            android:shell="true"
            tools:targetApi="29" />

        <!-- Media3 notification icon -->
        <meta-data
            android:name="androidx.media3.ui.PlayerNotificationManager.smallIcon"
            android:resource="@drawable/ic_notification" />
        
        <meta-data
            android:name="com.google.android.gms.car.notification.SmallIcon"
            android:resource="@drawable/ic_notification" />
        
        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Add MediaTransferReceiver for Output Switcher functionality.
             Must stay exported without a permission: the system and other
             media apps send media-transfer broadcasts to it by design. -->
        <receiver
            android:name="androidx.mediarouter.media.MediaTransferReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
        </receiver>
        


        <!-- Rhythm Music Widget (Modern Glance with Material 3) -->
        <receiver
            android:name=".infrastructure.widget.glance.RhythmWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name_glance"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_glance" />
        </receiver>

        <!-- Lyric Widget Provider (Glance-based) -->
        <receiver
            android:name=".infrastructure.widget.glance.RhythmLyricsWidgetReceiver"
            android:exported="true"
            android:label="Rhythm Lyrics"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lyric_widget" />
        </receiver>

        <!-- Rhythm Cookie Widget -->
        <receiver
            android:name=".infrastructure.widget.glance.RhythmCookieWidgetReceiver"
            android:exported="true"
            android:label="Rhythm Cookie"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/rhythm_cookie_widget_info" />
        </receiver>

        <!-- Rhythm Stats Widget -->
        <receiver
            android:name=".infrastructure.widget.glance.RhythmStatsWidgetReceiver"
            android:exported="true"
            android:label="Rhythm Stats"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/rhythm_stats_widget_info" />
        </receiver>
        
        <!-- MediaPlaybackService is exported so Android Auto, the system
             media controller and other apps can connect to the media session
             and MediaBrowserService. A permission would block those clients. -->
        <service
            android:name=".infrastructure.service.MediaPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
            <meta-data
                android:name="androidx.media3.ui.PlayerNotificationManager.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.session.MediaNotification.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.ui.NotificationUtil.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.session.DefaultMediaNotificationProvider.smallIcon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.ui.notification.small_icon"
                android:resource="@drawable/ic_notification" />
            <meta-data
                android:name="androidx.media3.session.notification.small_icon"
                android:resource="@drawable/ic_notification" />
        </service>
        
        <activity
            android:name=".activities.CrashActivity"
            android:exported="false"
            android:theme="@style/Theme.Rhythm"
            android:process=":crash_process" />

        <activity
            android:name=".activities.RhythmGuardTimeoutActivity"
            android:exported="false"
            android:theme="@style/Theme.Rhythm"
            android:launchMode="singleTop" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|keyboard|keyboardHidden|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_MUSIC" />
            </intent-filter>
            
            <!-- Handle playlists -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/x-mpegurl" />
                <data android:mimeType="audio/mpegurl" />
                <data android:mimeType="application/x-mpegurl" />
                <data android:mimeType="application/vnd.apple.mpegurl" />
                <data android:mimeType="application/vnd.apple.mpegurl.audio" />
            </intent-filter>
        </activity>
        
        <!-- External playback activity for quick audio file playback -->
        <activity
            android:name=".activities.ExternalPlaybackActivity"
            android:exported="true"
            android:label="Play Audio"
            android:theme="@android:style/Theme.DeviceDefault.Dialog.NoActionBar"
            android:launchMode="singleTask"
            android:excludeFromRecents="false"
            android:configChanges="orientation|screenSize|keyboardHidden">
            
            <!-- Handle VIEW intents for audio files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            
            <!-- Handle specific audio file types -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/mpeg" />
                <data android:mimeType="audio/mp3" />
                <data android:mimeType="audio/x-mp3" />
                <data android:mimeType="audio/mp4" />
                <data android:mimeType="video/mp4" />
                <data android:mimeType="audio/m4a" />
                <data android:mimeType="audio/aac" />
                <data android:mimeType="audio/x-aac" />
                <data android:mimeType="audio/ogg" />
                <data android:mimeType="audio/flac" />
                <data android:mimeType="audio/wav" />
                <data android:mimeType="audio/eac3" />
                <data android:mimeType="audio/mhm1" />
                <data android:mimeType="video/x-matroska" />
                <data android:mimeType="video/mkv" />
                <data android:mimeType="audio/x-matroska" />
                <data android:mimeType="audio/mka" />
                <data android:mimeType="audio/x-mka" />
                <data android:mimeType="application/x-matroska" />
            </intent-filter>
        </activity>

        <!-- Support per-app language switcher persistence on pre-Android 13 devices -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="androidx.appcompat.app.AppLocalesMetadataHolderService" />
            </intent-filter>
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Rhythm Quick Settings Tile Service -->
        <service
            android:name=".infrastructure.service.RhythmTileService"
            android:label="@string/app_name"
            android:icon="@drawable/ic_notification"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>
